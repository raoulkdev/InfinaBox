@tool
extends EditorPlugin
## Editor side of the InfinaBox addon. Intentionally empty for now: all of
## the addon's real work happens at runtime in the InfinaBox autoload
## (infinabox_runtime.gd), which InfinaBox registers in project.godot
## itself, so enabling or disabling this editor plugin doesn't change it.

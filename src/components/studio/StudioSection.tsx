// The Studio section: chat beside the Play and History panels (Phase A
// Task I fills this in and wires it into App.tsx).

export interface StudioSectionProps {
  projectPath: string | null;
}

export function StudioSection(_props: StudioSectionProps) {
  return null;
}
